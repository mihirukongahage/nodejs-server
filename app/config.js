const config = module.exports

// Server configurations
config.server = {
    host: 'localhost',
    port: 3000
}

// Database configurations
config.database = {
        host: 'localhost',
        user: 'root',
        password: '',
        database: 'notes-manager',
}